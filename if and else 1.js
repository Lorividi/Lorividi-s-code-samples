'use strict'


    let answer;
        answer = prompt ('What is official name of JavaScript');

        if (answer == 'EcmaScript') {
            alert ('True, nice job, pal!')
        } else if (answer == 0) {
            alert ('cancel by user...')
        } else {
            alert ('Nope, dude, it is EcmaScript!')
        }

/* Используя конструкцию if..else, 
напишите код, который будет спрашивать: „Какое «официальное» название JavaScript?“
Если пользователь вводит «ECMAScript», 
то показать: «Верно!», в противном случае – отобразить: «Не знаете? ECMAScript!» */
